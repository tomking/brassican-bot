// TODO

// Check that the command caller has a role in the staff roles list

// Call (and await) to update all clan members on WOM
// Query (and await) the DB, pulling everything full members collection
// Iterate through members for each:
// Call (and await) update member function
